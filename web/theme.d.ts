/**
 * Pulls in the design-system MUI theme augmentations so consumers of `useTheme`
 * get the extended palette typings without importing `@mui/material` directly.
 */
import "@taskflow/themes/types";
