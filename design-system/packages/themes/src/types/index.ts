/**
 * Product-specific theme type augmentations.
 *
 * Import this file to extend MUI theme types with the optional palette extensions
 * the design system supports (e.g. extra `50..900` ramps on `PaletteColor`).
 *
 * @example
 * ```ts
 * // In your app's entry point or a global type-declaration file:
 * import '@taskflow/themes/types';
 * ```
 */

import './taskflow.js';

export {};
